# Lucid Remote Desktop — host dependencies
# pip install -r requirements.txt

# Async HTTP + WebSocket signaling
aiohttp>=3.9.0

# WebRTC stack (RTP / UDP / ICE / DTLS / SCTP for data channel)
aiortc>=1.6.0

# H.264 encoding (bundled with aiortc, but pinned for clarity)
av>=12.0.0

# DXGI screen capture (GPU-accelerated Desktop Duplication API)
dxcam>=0.0.5

# OpenCV — required by dxcam's cv2_processor for BGR colour conversion
# (without it, dxcam silently fails to produce frames)
opencv-python>=4.8.0

# Screen capture fallback (pure Python, no C++ deps)
mss>=9.0.1

# Audio bridge (browser mic → VB-Cable)
sounddevice>=0.4.6

# Numerical frame processing
numpy>=1.24.0

# Self-signed TLS cert generation (for WebCodecs secure context requirement)
cryptography>=42.0.0
